import {curryWrap, curriedComponentWise} from './helpers'

export const dot = curryWrap((v1, v2) => ({}))

export const cross = curryWrap((v1, v2) => ({}))

export const length = v => {}

export const normalize = v => {}
